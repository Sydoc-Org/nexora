Trend shape only — no axes, no labels, no tooltip. Use inside `Metric`; for anything a reader must read values off, use a real chart.

```jsx
<Sparkline values={[420,500,470,520,560,480,595]} color="var(--nx-series-1)" />
```
