---
description: Alias for /issue — look up a GitHub issue and report back before touching code
argument-hint: "<issue_number>"
---

Run the `/issue` workflow for issue `$1`: read `.claude/commands/issue.md` and follow it exactly, with `$1` = `$1`.
