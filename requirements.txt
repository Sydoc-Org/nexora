# nexora — direct dependencies only.
# Transitive dependencies (Jinja2, MarkupSafe, click, blinker, WTForms,
# cachelib, limits, certifi, urllib3, idna, charset-normalizer, cryptography,
# etc.) are resolved automatically by pip.
#
# Versions match what is currently running in production.
# After editing this file, run:
#     pip install -r requirements.txt --upgrade
# Or recreate the venv from scratch:
#     python -m venv venv && venv\Scripts\pip install -r requirements.txt

# --- Flask core ---
Flask==3.1.2
Werkzeug==3.1.3
itsdangerous==2.2.0

# --- Flask extensions ---
flask-babel==4.0.0
Flask-Caching==2.3.1
Flask-Limiter==3.12
Flask-Session==0.8.0
flask-talisman==1.1.0
Flask-WTF==1.2.2

# --- Database ---
pyodbc==5.2.0
SQLAlchemy==2.0.45

# --- Security / auth ---
bcrypt==4.3.0
pyotp==2.9.0
qrcode==8.2

# --- HTTP / env ---
python-dotenv==1.1.1
requests==2.32.4

# --- Files / images ---
pillow==11.3.0
python-magic-bin==0.4.14
